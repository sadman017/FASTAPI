services:
  api:
    build: .
    container_name: smart_bite_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    # If you need persistent SQLite storage outside the container:
    # volumes:
    #   - ./data:/app/data
